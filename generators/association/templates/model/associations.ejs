<% if (association === 'belongsToMany') { -%>
    model.<%= association %>(db.<%= target %>, {
        through: db.<%= through %>
    });
<% } else {-%>
    model.<%= association %>(db.<%= target %>);
<% } -%>